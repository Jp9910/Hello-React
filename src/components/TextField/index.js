import { TextField } from "./TextField";

export default TextField // Sets what will be exported when exporting this file

// This file will handle exporting the component (TextField)
// Here it is chosen which components to export, so that there isn't unused or global stuff.